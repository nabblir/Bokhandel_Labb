<Window x:Class="Bokhandel_Labb.Views.RedigeraBokView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        xmlns:local="clr-namespace:Bokhandel_Labb.Views"
        mc:Ignorable="d"
        FontFamily="Bahnschrift SemiLight"
        Title="Lägg till bok"
        Height="600" 
        Width="700"
        WindowStartupLocation="CenterOwner"
        Background="DarkSlateGray"
        ResizeMode="NoResize">
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <!-- Boktitel -->
        <StackPanel Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="Boktitel:" 
                       FontSize="12"
                       Margin="0,0,0,5" Foreground="White"/>
            <TextBox Text="{Binding BokTitel, UpdateSourceTrigger=PropertyChanged}" 
                     Height="30"
                     VerticalContentAlignment="Center"
                     FontSize="14"
                     Padding="5"/>
        </StackPanel>
        <!-- ISBN -->
        <StackPanel Grid.Row="1" Margin="0,0,0,15">
            <TextBlock Text="ISBN:"
                       FontSize="12"
                       Margin="0,0,0,5" Foreground="White"/>
            <TextBox Text="{Binding ISBN, UpdateSourceTrigger=PropertyChanged}" 
                     Height="30"
                     VerticalContentAlignment="Center"
                     FontSize="14"
                     Padding="5"/>
        </StackPanel>
        <!-- Författare -->
        <StackPanel Grid.Row="2" Margin="0,0,0,15">
            <TextBlock Text="Författare:"
                       FontSize="12"
                       Margin="0,0,0,5" Foreground="White"/>
            <TextBox Text="{Binding Forfattare, UpdateSourceTrigger=PropertyChanged}" 
                     Height="30"
                     VerticalContentAlignment="Center"
                     FontSize="14"
                     Padding="5"/>
        </StackPanel>
        <!-- Språk -->
        <StackPanel Grid.Row="3" Margin="0,0,0,15">
            <TextBlock Text="Språk:"
                       FontSize="12"
                       Margin="0,0,0,5" Foreground="White"/>
            <TextBox Text="{Binding Språk, UpdateSourceTrigger=PropertyChanged}" 
                     Height="30"
                     VerticalContentAlignment="Center"
                     FontSize="14"
                     Padding="5"/>
        </StackPanel>
        <!-- Antal -->
        <StackPanel Grid.Row="4" Margin="0,0,0,15">
            <TextBlock Text="Antal:"
                       FontSize="12"
                       Margin="0,0,0,5" Foreground="White"/>
            <TextBox Text="{Binding BokAntal, UpdateSourceTrigger=PropertyChanged}" 
                     Height="30"
                     VerticalContentAlignment="Center"
                     FontSize="14"
                     Padding="5"/>
        </StackPanel>
        <!-- Butik -->
        <StackPanel Grid.Row="5" Margin="0,0,0,15">
            <TextBlock Text="Välj Butik:" 
                       FontSize="12"
                       Margin="0,0,0,5" Foreground="White" />
            <ComboBox ItemsSource="{Binding Butik}"
                      SelectedItem="{Binding ValdButik}"
                      DisplayMemberPath="ButiksNamn"
                      Height="30"
                      Padding="5"
                      FontSize="14"/>
        </StackPanel>
        <!-- Status meddelande -->
        <TextBlock Grid.Row="7"
                   Text="{Binding StatusMeddelande}"
                   FontSize="12"
                   Foreground="{Binding StatusTextFärg}"
                   FontWeight="SemiBold"
                   HorizontalAlignment="Center"
                   TextWrapping="Wrap"
                   Margin="0,5,0,5" FontFamily="Bahnschrift SemiLight"/>
        <!-- Footer -->
        <StackPanel Grid.Row="8" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right"
                    Margin="0,10,0,0">
            <Button Content="Avbryt" 
                    Command="{Binding CancelCommand}"
                    Width="80" 
                    Height="35"
                    Margin="0,0,10,0"
                    IsCancel="True"
                    Background="#FFF59696"
                    FontWeight="SemiBold" 
                    BorderBrush="Black" FontFamily="Bahnschrift SemiLight"/>
            <Button Content="Lägg Till" 
                    Command="{Binding OKCommand}"
                    Width="80" 
                    Height="35"
                    IsDefault="True"
                    Background="#FF96F596"
                    FontWeight="SemiBold" 
                    BorderBrush="Black" 
                    IsEnabled="{Binding EnableLäggTillBok}" FontFamily="Bahnschrift SemiLight"/>
        </StackPanel>
    </Grid>
</Window>